# Pinned for reproducible builds. Direct dependencies only; pip resolves the
# transitive tree from these.
fastapi==0.139.0
uvicorn[standard]==0.51.0
fastapi-users[sqlalchemy]==15.0.5
sqlalchemy==2.0.51
sqladmin==0.28.0
# sqladmin 0.28's BooleanInputWidget breaks on WTForms 3.2 (base Input dropped
# `validation_attrs`); pin below 3.2 until sqladmin fixes it.
wtforms==3.1.2
jinja2==3.1.6
# Server-rendered login/register forms (fastapi.Form) need multipart parsing.
python-multipart==0.0.32
# sqladmin's admin session (SessionMiddleware) signs cookies with this.
itsdangerous==2.2.0
pydantic-settings==2.14.2
alembic==1.18.5
# Server-side fetch of the user's submissions for the home page.
httpx==0.28.1
# Database drivers: asyncpg for Postgres (prod/compose), aiosqlite for the
# local SQLite dev fallback.
asyncpg==0.31.0
aiosqlite==0.22.1
