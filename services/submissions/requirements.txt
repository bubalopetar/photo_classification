# Pinned for reproducible builds. Direct dependencies only; pip resolves the
# transitive tree from these.
fastapi==0.139.0
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
sqladmin==0.28.0
# sqladmin 0.28's BooleanInputWidget breaks on WTForms 3.2 (base Input dropped
# `validation_attrs`); pin below 3.2 until sqladmin fixes it.
wtforms==3.1.2
jinja2==3.1.6
# Imported directly in app/admin.py (Markup for the photo/email formatters).
markupsafe==3.0.3
# Multipart upload parsing (photo + form fields).
python-multipart==0.0.32
# Sync REST call to the Classification service.
httpx==0.28.1
# Local JWT verification (libs/shared/shared/security.py).
pyjwt==2.13.0
# Admin session cookies (SessionMiddleware) are signed with this.
itsdangerous==2.2.0
pydantic-settings==2.14.2
alembic==1.18.5
# Database drivers: asyncpg for Postgres (prod/compose), aiosqlite for the
# local SQLite dev fallback.
asyncpg==0.31.0
aiosqlite==0.22.1
# Image validation / EXIF stripping (upload safety gate).
pillow==12.3.0
